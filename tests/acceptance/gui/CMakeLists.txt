# GUI acceptance tests — coming soon.
#
# Strategy:
#   Level 1: Solver logic is fully covered via sudoku_solver_lib unit tests.
#   Level 2: Widget interaction via imgui_test_engine (headless, no window needed).
#            Add when GUI implementation is in place.